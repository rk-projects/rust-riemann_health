### Trying rust implementation of riemann_health.

#### Uses riemann_client rust plugin 

Currently, riemann_client plugin is not from crates.io, it's from a local compiled version created by @SanchayanMaity. 